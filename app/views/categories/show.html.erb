  <!-- Courier Table -->

<div class="container fluid">
  <h1>Courier</h1>
  <div class="row">
    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>Category Type</th>
          <th>Disease</th>
          <th>Medicine</th>
          <th colspan="2">Actions</th>
        </tr>
      </thead>
    <tbody>
      <tr>
        <td><%= @category.name %></td>
        <td><%= @category.disease.name %></td> 
        <td><%= @category.medicine.name %></td>   
        <td><%= link_to "Edit", edit_category_path, class: "btn btn-warning float-right" %><br /></td>
        <td><%= button_to "Delete", category_path, method: :delete, class: "btn btn-danger float-right" %></td>
      </tr>
    </tbody>
    </table>
  </div>
    <%= link_to "Add New Category", new_category_path, class: "btn btn-primary" %>
</div>
