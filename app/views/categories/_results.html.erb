              <!--Searched Category Table-->
  
<% if @search_category.present? %>
  <ul>
    <% @search_category&.each_with_index do |category, index| %>
    <li>
      <%= link_to category.name, category_url(category)%>
    </li>
<% end %>
  </ul>
<% end %>
