                  <!--Courier Form-->
<%= simple_form_for @courier do |f| %>
  <% if @courier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@courier.errors.count, "error") %> prohibited this courier from being saved:</h2>

      <ul>
        <% @courier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <div class="container-fluid">
      <div class="row justify-content-center">
        <div class="col-4">
          <div class="card">
            <div class="card-body">
              <h2> Courier Details </h2>
              <div class="mb-3">
                <%= f.label :name, { class: "form-label"} %>
                <%= f.text_field :name, { class: "form-control", placeholder: "Courier Name" } %>
              </div>

              <div class="mb-3">
                <%= f.label :address, { class: "form-label"} %>
                <%= f.text_field :address, { class: "form-control", placeholder: "Courier Address" } %>
              </div>

              <div class="mb-3">
                <%= f.label :code, { class: "form-label"} %>
                <%= f.text_field :code, { class: "form-control", placeholder: "Courier Code" } %>
              </div>

              <div class="mb-3">
                <%= f.label :contact_no, { class: "form-label"} %>
                <%= f.text_field :contact_no, { class: "form-control", placeholder: "Courier Contact Number" } %>
              </div>

              <div class="mb-3">
                <%= f.submit "Save", class: "btn btn-primary" %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>            
  
<% end %>
